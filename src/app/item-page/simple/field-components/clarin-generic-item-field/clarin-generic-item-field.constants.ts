/**
 * Metadata field constants for the CLARIN generic item field.
 * Extracted from the component to avoid a circular dependency with
 * shared/item-identifier.service.
 */
export const DOI_METADATA_FIELD = 'dc.identifier.doi';
export const HANDLE_METADATA_FIELD = 'dc.identifier.uri';
